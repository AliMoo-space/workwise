import 'package:flutter/material.dart';

class PolicyAssistant extends StatelessWidget {
  const PolicyAssistant({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('PolicyAssistant'));
  }
}
